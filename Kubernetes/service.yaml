apiVersion: v1
kind: Service

metadata:
  name: football-portal-service

spec:
  type: LoadBalancer

  selector:
    app: football-portal

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080